# Dependencies
node_modules/

# Build output (regenerated by `npm run build` / `sdk-commands start`)
bin/

# Decentraland SDK local caches
.dclcache/
*.tsbuildinfo
# Local Asset Bundles output (`npm start -- --asset-bundles`). Generated from
# assets/ on demand and must never be committed OR deployed — it is a build
# artifact the size of the source assets.
.dcl-optimized-assets/

# OS / editor cruft
.DS_Store
*.log
npm-debug.log*
.vscode/
.idea/
