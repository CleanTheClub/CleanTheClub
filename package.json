{
  "name": "dcl-project",
  "version": "1.0.0",
  "description": "SDK7 Template scene",
  "scripts": {
    "start": "sdk-commands start",
    "deploy": "sdk-commands deploy",
    "build": "sdk-commands build",
    "predeploy": "echo \"→ PRE-DEPLOY: confirm PROGRESS_BIN_ID/KEY and LEADERBOARD_BIN_ID/KEY are set for this world, and after publishing check the boot log for 'persistence: external store (jsonbin)'. See DEPLOY.md.\"",
    "upgrade-sdk": "echo \"BLOCKED: see UPGRADE-SDK.md — the auth-server tag moves, and re-resolving it silently walks the pin onto a build nobody has tested this scene against. Read the migration notes first, then install the exact version by hand.\" && exit 1",
    "upgrade-sdk:next": "echo \"BLOCKED: see UPGRADE-SDK.md — 'next' moves with every SDK build and is NOT the auth-server line the multiplayer server runtime depends on. Read the migration notes first, then install the exact version by hand.\" && exit 1",
    "server-logs": "sdk-commands sdk-server-logs",
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@dcl/asset-packs": "2.17.0",
    "@dcl/js-runtime": "7.26.1-32160793830.commit-0b97733",
    "@dcl/sdk": "7.26.1-32160793830.commit-0b97733",
    "@types/node": "22.10.5",
    "vitest": "2.1.9"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 120,
    "trailingComma": "none"
  }
}
