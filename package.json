{
  "name": "dcl-project",
  "version": "1.0.0",
  "description": "SDK7 Template scene",
  "scripts": {
    "start": "sdk-commands start",
    "deploy": "sdk-commands deploy",
    "build": "sdk-commands build",
    "upgrade-sdk": "echo \"BLOCKED: see UPGRADE-SDK.md — the auth-server tag may now rebase onto 7.26.0, which changes UI scaling. Read the migration notes first, then run the install manually.\" && exit 1",
    "upgrade-sdk:next": "npm install --save-dev @dcl/sdk@next",
    "server-logs": "sdk-commands sdk-server-logs",
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "@dcl/asset-packs": "2.17.0",
    "@dcl/js-runtime": "7.25.1-30310486734.commit-5ffe873",
    "@dcl/sdk": "7.25.1-30310486734.commit-5ffe873"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 120,
    "trailingComma": "none"
  }
}